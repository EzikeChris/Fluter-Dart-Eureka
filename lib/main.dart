import 'package:flutter/material.dart';

// The starting point for all apps
void main() => runApp(
      MaterialApp(
        home: Center(
          child: Text('Hello World'),
        ),
      ),
    );
